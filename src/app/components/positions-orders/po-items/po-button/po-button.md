# Po-metrics

Пропсы:

- `buttonType: String` - Пропс отвечает за тип кнопки. Принимает одно из двух значений `Edit, Close`.

За что отвечает:

- Кнопка контроля

Где используется?

- Positions-Orders

```ts
<po-button
  buttonType="Edit"
>
```
