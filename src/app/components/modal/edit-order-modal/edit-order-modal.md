# Edit-order-modal

Пропсы:

- `newPosition: Position` - Пропс отвечает за данные редактируемой позиции

За что отвечает:

- Модальное окно для редактирования открытого ордера

Где используется?

- Positions-Orders

```ts
<edit-order-modal
  newPosition="Position Object"
>
```
